# Copy to .env.local for dev; set the same key in the Vercel project for deploys.

# Where the API is. Inlined at build time, so changing it in Vercel needs a redeploy.
# Leave it empty to fetch relative to the dashboard's own origin.
NEXT_PUBLIC_API_URL=http://localhost:8787
